{
  "name": "turborepo-basic-yarn",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dx": "run-s docker:up dev",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "docker:up": "docker-compose -f docker-compose.yml up --build -d",
    "test": "turbo run test",
    "test:ci": "turbo run test:ci",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "2.7.1",
    "npm-run-all": "4.1.5",
    "turbo": "1.2.16"
  }
}
